<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elejunx.hungers">
    <!-- add these nodes between manifest and application  -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/hunger_logo_small"
        android:label="@string/app_name"
        android:roundIcon="@drawable/hunger_logo_small"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hungers">
        <activity android:name=".Profile"></activity>
        <activity android:name=".OrderHistory" />
        <activity android:name=".Order" />
        <activity android:name=".Checkout" />
        <activity android:name=".AddAddress" />
        <activity android:name=".Address" />
        <activity android:name=".Cart" />
        <activity android:name=".ResetPass" />
        <activity android:name=".OTP" />
        <activity android:name=".ForgotPass" />
        <activity android:name=".Home" />
        <activity android:name=".SignUp" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>